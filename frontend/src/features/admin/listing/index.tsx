import * as Posts from "./posts";
import * as Forms from "./forms";
import * as FormEntries from "./form-entries";
import * as Users from "./users";

const Listing = {
  Posts,
  Forms,
  FormEntries,
  Users,
};
export default Listing;
